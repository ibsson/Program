#include <stdio.h>

int compare(char string_1[], char string_2[]) {

	for (int i = 0; i < 101; i++) {
		if (string_1[i] == string_2[i]) {
			continue;
		}
		else {
			return 0;
			break;
		}
	}

	return 1;
}

int main() {
	char string_1[101];
	char string_2[101];

	scanf_s("%s", string_1, 101);
	scanf_s("%s", string_2, 101);

	if (compare(string_1, string_2)) {
		printf("they are same.");
	}
	else {
		printf("they are not same.");
	}

	return 0;
}
