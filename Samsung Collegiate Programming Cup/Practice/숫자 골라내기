#include <iostream>
using namespace std;

int T, N;

int main(void) {
	ios::sync_with_stdio(0);
	cin.tie(0);

	cin >> T;
	
	for (int t = 1; t <= T; t++) {
		cin >> N;

		int x = 0, result = 0;

		for (int i = 0; i < N; i++) {
			cin >> x;

			result ^= x;
		}

		cout << "Case #" << t << "\n";
		cout << result << "\n";
	}

	return 0;
}
